{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "prettier-plugin-svelte": "^3.2.2",
    "turbo": "^2.0.4"
  },
  "engines": {
    "node": ">=18"
  },
  "name": "with-svelte",
  "packageManager": "bun@1.1.13",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
